<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.films">

    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:name=".FilmApp">
        <activity android:name=".presentation.splash.SplashActivity"
                  android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".presentation.main.MainActivity"/>
        <activity android:name=".presentation.search.SearchActivity"/>
        <activity android:name=".presentation.reminders.RemindersActivity" />
        <activity android:name=".presentation.movielist.MovieListActivity" />
        <activity android:name=".presentation.signup.SignUpActivity"/>
        <activity android:name=".presentation.login.LoginActivity" />
        <activity android:name=".presentation.auth.AuthMenuActivity" />

        <service
                android:name="com.example.films.jobs.ReminderJob"
                android:permission="android.permission.BIND_JOB_SERVICE"/>

        <meta-data
                android:name="com.google.android.gms.ads.APPLICATION_ID"
                android:value="@string/ad_mob_app_id"/>
    </application>

</manifest>
